# Python
.venv/
__pycache__/
*.pyc
*.pyo

# Secrets (only the .env.example template is committed)
.env
.env.*
!.env.example

# Local CLI install artifacts (e.g. the Bob CLI tarball)
*.tgz

# Generated run artifacts (reproducible — see README)
runs/
results/
*.duckdb

# OS / editor
.DS_Store
